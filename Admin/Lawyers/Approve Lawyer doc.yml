info:
  name: Approve Lawyer doc
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}/api/v1/admin/lawyers/{lawyerCredential}/approve"
  auth:
    type: bearer
    token: "{{access_token_admin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
