info:
  name: Show on Submission
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/api/v1/admin/lawyers/b8ac8431-7e9f-44ea-86c3-50fa7b6fa047"
  auth:
    type: bearer
    token: "{{admin_token_local}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: |-
  this id in url is user id \
  because user can't upload more than on doc(License)
