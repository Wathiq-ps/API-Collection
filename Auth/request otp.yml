info:
  name: request otp
  type: http
  seq: 3
  tags:
    - Wathiq/Auth

http:
  method: POST
  url: "{{URL}}/api/v1/auth/otp/request"
  body:
    type: json
    data: '"{\n  \"email\": \"albaraawork3@gmail.com\",\n  \"status\": \"register\" //login and register\n}"'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/auth/otp/request"
      method: POST
      body:
        type: json
        data: '"{\n  \"email\": \"albaraawork3@gmail.com\",\n  \"status\": \"register\" //login and register\n}"'
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
