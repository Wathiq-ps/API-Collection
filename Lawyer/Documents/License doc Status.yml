info:
  name: License doc Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/v1/lawyer/credentials/status"
  auth:
    type: bearer
    token: "{{lawyer_token_local}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
