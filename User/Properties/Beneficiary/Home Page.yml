info:
  name: Home Page
  type: http
  seq: 2
  tags:
    - Wathiq/User/Properties/Beneficiary

http:
  method: GET
  url: "{{URL}}/api/v1/properties/top-rated"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: https://wathiq-back.up.railway.app

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/properties/top-rated"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
