info:
  name: My Requests
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/v1/properties/my-requests"
  auth:
    type: bearer
    token: "{{local_user_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
