info:
  name: Search
  type: http
  seq: 1
  tags:
    - Wathiq/User/Properties/Beneficiary

http:
  method: GET
  url: "{{URL}}/api/v1/properties/search"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: Successful response
    request:
      url: "{{baseUrl}}/api/v1/properties/search"
      method: GET
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
