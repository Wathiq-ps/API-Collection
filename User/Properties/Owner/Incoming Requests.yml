info:
  name: Incoming Requests
  type: http
  seq: 7

http:
  method: GET
  url: "{{URL}}/api/v1/properties/incoming-requests"
  auth:
    type: bearer
    token: "{{access_token_user}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
