info:
  name: Reject Request
  type: http
  seq: 8

http:
  method: PATCH
  url: "{{URL}}/api/v1/properties/requests/9d915e1c-0219-49ad-bc9d-186069695413/reject"
  body:
    type: json
    data: |-
      {
        "reason": "this is for test"
      }
  auth:
    type: bearer
    token: "{{access_token_user}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
